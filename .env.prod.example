# Copy this file to .env.prod before starting the production compose stack.
# Keep .env.prod out of git. Generate strong values with: openssl rand -hex 32
POSTGRES_PASSWORD=
AUTH_SECRET=

# Add the remaining runtime variables from .env.example here as needed.
